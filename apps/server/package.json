{
  "name": "@remux/server",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/websocket": "^11.0.1",
    "@remux/config": "0.1.2",
    "@remux/protocol": "0.1.2",
    "fastify": "^5.3.3",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "tsx": "^4.19.4"
  }
}
